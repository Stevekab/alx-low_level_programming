#include "main.h"
/**
 * mul - multiply
 * @x: the first number to multiply
 * @y: the sec number to be multiplied
 * Return: the result of the multplication between x and y parameters
 */
int mul(int x, int y)
{
	return (x * y);
}
